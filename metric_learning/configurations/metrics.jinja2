[
{% if recall | default(True) %}
    {
        'name': 'recall',
        'compute_period': {{ compute_period | default(200) }},
        'k': {{ recall_k | default([1, 2, 4, 8]) }},
        'batch_size': 48,
    },
{% endif %}
{% if accuracy | default(False) %}
    {
        'name': 'accuracy',
        'compute_period': {{ compute_period | default(200) }},
        'batch_size': 48,
    },
{% endif %}
]
