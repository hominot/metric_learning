{
    'learning_rate': {{ learning_rate | default(0.0001) }},
    'num_epochs': {{ num_epochs | default(100) }},
{% if lr_embedding %}
    'lr_embedding': {{ lr_embedding }},
{% endif %}
{% if lr_decay_steps %}
    'lr_decay_steps': {{ lr_decay_steps }},
    'lr_decay_rate': {{ lr_decay_rate | default(0.5) }},
{% endif %}
    'combine_batches': {{ combine_batches | default(5) }}
}